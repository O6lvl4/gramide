// Generated by scripts/gen_grammar_tables.py.
// Every package that ships a compiled grammar, by package id.
import self.packages.tables.almide as table_almide
import self.packages.tables.go as table_go
import self.packages.tables.rust as table_rust
import self.packages.tables.python as table_python
import self.parser

fn compiled_for(id: String) -> Option[parser.Compiled] =
  if id == "almide" then some(table_almide.compiled())
  else if id == "go" then some(table_go.compiled())
  else if id == "rust" then some(table_rust.compiled())
  else if id == "python" then some(table_python.compiled())
  else none
